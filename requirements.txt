fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-dotenv>=1.1.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# LLM providers
anthropic>=0.42.0
openai>=1.60.0
groq>=0.15.0

# Zero-Mem memory (arXiv:2607.29377) is stdlib-only (sqlite3, re, math).
# Embeddings and entity/relation extraction run in the cloud via the Gemini
# API over httpx — no local ML stack, no model downloads.
#
# Optional, only if you deliberately want local embeddings instead of the
# cloud (pulls torch + a multi-GB model):
#   pip install "sentence-transformers>=3.4.0"

# Vetted historical corpus (backend/history) is YAML on disk.
pyyaml>=6.0

# Text processing
tiktoken>=0.8.0
markdown>=3.7

# HTTP
httpx>=0.28.0
aiofiles>=24.1.0

# Utilities
jinja2>=3.1.5
python-multipart>=0.0.20
